# Toolchain

This directory will hold the Astra compiler toolchain.

Planned components:

- parser
- binder
- type checker
- effect analyzer
- artifact graph builder
- emitters


## New emitters

- `ts-client` — generates a typed TypeScript SDK scaffold
- `server-fastapi` — generates FastAPI/Pydantic server stubs
